1.面向对象就是不懂其原理，但是我会用,用好一个功能
OOP 面向对象的特点：
one:抽象--把最主要的功能和特征抽出来
封装--看不到里面的东西，用好表面的功能
继承--遗传

多重继承
多态


2.Tab
如何把一个面向过程的程序改成面向对象


   a.window.onload   改成构造函数  函数嵌套提出来
   b.初始化整个程序    初始化整个对象
   c.变量改成属性
   d.函数改成方法


3.系统对象
本地对象：（非静态对象）
常用对象：Object Function Array String Boolean Number Date RegExp Error
内置对象（静态对象）
Global,Math
宿主对象：DOM BOM
